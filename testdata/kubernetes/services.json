{
  "items": [
    {
      "metadata": {
        "name": "nginx",
        "namespace": "default"
      },
      "spec": {
        "type": "ClusterIP",
        "ports": [
          {"port": 80, "targetPort": 80, "protocol": "TCP"}
        ],
        "selector": {
          "app": "nginx"
        }
      }
    },
    {
      "metadata": {
        "name": "postgres",
        "namespace": "default"
      },
      "spec": {
        "type": "ClusterIP",
        "ports": [
          {"port": 5432, "targetPort": 5432, "protocol": "TCP"}
        ],
        "selector": {
          "app": "postgres"
        }
      }
    },
    {
      "metadata": {
        "name": "grafana",
        "namespace": "monitoring"
      },
      "spec": {
        "type": "NodePort",
        "ports": [
          {"port": 3000, "targetPort": 3000, "nodePort": 30300, "protocol": "TCP"}
        ],
        "selector": {
          "app": "grafana"
        }
      }
    }
  ]
}
